# Figma parity: frontend contract handoff

Date: 2026-09-03. Repository: `picapicaowo-alt/coursistant-lisa-IELTS`.

This document replaces the earlier uncommitted draft. That draft described simulated scores, dates, and actions as delivered behavior. Those claims are withdrawn. The evidence below is the **checked-in frontend OpenAPI snapshot**, not an authenticated backend acceptance result. No backend, database, service, or infrastructure change is included.

The companion [Figma inventory](figma-parity-audit-2026-09-03.md) maps every screen/state to a frontend route. The identifiers below are traceable dependencies, not proposed endpoints represented as existing APIs.

| ID | Figma requirement | Existing contract / evidence | Missing information or capability | Current frontend behavior |
|---|---|---|---|---|
| B01 | Course cards and current lesson: lesson progress, next class, time remaining, resume | `GET /v2/me/progress` (`StudentProgressResponse`); course weeks/materials; course sessions; `GET /v2/me/calendar` | Student progress counts **assignments**, not lessons watched. No persisted material playback position, learning duration, or course-local resume pointer is specified. Calendar's generic read needs an explicit typed occurrence projection. | Student cards label assignment completion accurately. Weekly class templates are labelled weekly; calendar reads dated occurrences. Material reader opens a selected material, never claims it was the last viewed lesson. Advisor course cards use the separately supplied `lectureCompleted/lectureTotal`. |
| B02 | Course Note tab | No course-note collection/write operation in the 11 consumed snapshots | Persisted note ID, course/material association, body format, ownership, pagination, conflict/version behavior, and delete semantics | No simulated saved notes or browser-only records presented as synchronized course notes. |
| B03 | Discussion identities, likes/dislikes, typed attachment/reply rendering | `GET/POST /v2/courses/{courseId}/discussion/posts`, detail, replies and attachment read/download/preview exist in `course.openapi.yaml`; read envelope is generic `ApiResponse` | Typed post/reply author display, role, timestamp, body, pagination/total, attachment shape; no like/dislike mutation specified | Post, thread, replies and attachments use existing requests with idempotency. Generic records are guarded; missing IDs never become clickable inferred IDs. No made-up author, like count, or like action. |
| B04 | Advisor dashboard AI panel and longitudinal charts | `GET /v2/advisor/dashboard`, action tasks, conversation summaries and schedule requests exist | No advisor AI conversation endpoint or scored cohort time series with metric, sample/denominator and period definition | Dashboard shows real summary counts and operational queues. The lavender panel provides real advising navigation. No chart built from fabricated marks or hardcoded student cohorts. |
| B05 | Advisor Messages: independent Parents directory/thread | `GET /v2/advisor/students/{studentUserId}/parent-links` exists. Parent-facing `/v2/parent/students/{studentUserId}/conversation/*` exists. Advisor conversation read/send addresses **studentUserId only**. | Advisor-side parent conversation directory, parent-specific thread identity/routing and permission definition; clarify whether the parent thread is shared with the student thread or separate. Parent-links alone cannot authorize sending to a parent-specific thread. | Existing advisor parent-link read remains available in the student workspace. Messages has a real student directory and student-scoped conversation. No parent-facing endpoint is called with an Advisor identity. |
| B06 | Students: numeric current level/delta, next checkpoint, Figma High/Medium/Low risk | `AdvisorStudentSummaryResponse` has identity, goal, risk status/reasons, last activity and task summary | List projection lacks numeric latest assessment with scale/date and next-checkpoint date. Risk codes are `ON_TRACK`, `AT_RISK`, `NEEDS_ATTENTION`; severity equivalence with High/Medium/Low is unspecified. Bulk assign/export behavior is also unspecified. | Missing assessment and checkpoint values display Not available. Labels retain actual risk meanings; missing risk is neutral. IDs are actual IDs. No `6.5`, `-1.0`, invented `STU2026...`, fake export, or local-only assignment. |
| B07 | Advisor course detail: schedules and per-assignment performance | `AdvisorStudentCourseResponse` supplies course identity, instructor(s), lecture counts and `schedule: unknown[]`; advisor published course report reads exist | Typed schedule rows and advisor student-specific assessment/assignment performance projection with release visibility. Course delivery configuration is owner administration, not a generic student-course detail permission. | View Course modal has information/schedule tabs using the assigned student's returned data. Existing linking, one-to-one creation, launch, reconfirmation and completion mutations keep their versions and authorization boundaries. No alternate student endpoint is used to read grades. |
| B08 | Assign exam: scheduled Listening/Reading/Writing/Speaking dates and times | `createAdvisorStudentExam` request has templateId, section selections, optional writingInstructorUserId | No planned start/end or Speaking section in that request. Need schedule mutation, timezone, availability and version semantics before enabling scheduling | Assign exam modal uses published templates and actual selectable sections. It does not claim assignment means a scheduled sitting. |
| B09 | Advisor exam review modal: question-level marking and editing | Advisor observer detail (`ObserverMockExamDetail`) has section selections, listening/reading correct/total and writing score/status. Instructor Writing grading is a separate authorized workflow. | Observer response lacks question/answer/feedback detail and an Advisor mutation for editing those results | Observer result modal displays supplied scores only. No student-attempt route or Instructor grading request is offered under Advisor/Parent permissions. |
| B10 | Calendar participant chips, event description and personal reminder behavior | `PersonalEventRequest`: title, startsAtLocal, endsAtLocal, timezone, reminderMinutesBefore, expectedVersion. `GET /v2/me/personal-events` requires fromUtc/toUtc; `GET /v2/me/calendar` uses date-only half-open `[from,to)` | Description/participant fields are absent. Personal and unified calendar reads still use generic responses; provide typed item schemas, timezone representation and pagination guarantees | Create/edit/refetch/delete use the documented requests, retain local whole seconds, and never infer a missing version. Overlapping events occupy separate lanes. Incomplete date/time records are reported, not made into events. |
| B11 | Student task opens a linked quiz (430:2779) | `AdvisorTaskResponse` includes task identity, title/description, deadline, status, version, submission and advisor feedback | No linked quiz/course assignment ID or authorized destination in the task response | Task details and start/complete actions work. No quiz ID is fabricated from task IDs or free text. |
| B12 | AI history/search/share, image generation, deep research, examination AI explanation | Existing Study Support / Instructor assistant stream and Workflow contracts | No consumed persistent chat-history/share/image/deep-research contract for this workspace; exam explanation capability needs explicit exam-context and lockdown policy | Existing grounded chat/workflow remain operational with course scope. No fake historical threads, public share success, or new tools inferred from an Figma icon. |
| B13 | Figma activation/social sign-in and profile labels | Auth snapshot supports email/code registration/reset, structured names, tenant selection. Student creation is governed by the intake flow. Profile and student-facing learning profile are separate reads | No generic social OAuth/activation-token operation matching every decorative option; no numeric conversion rule for free-text baseline or a Member since field in learning profile | Existing supported registration/reset states retain contract requirements; profile uses split names and measured skills. Avatar cropping is performed locally before the real avatar upload. Released assessment cards use real `my-grades`, not sample IELTS band cards. |

| B14 | System Admin tenant directory and maintenance | The current Auth/User snapshots provide managed-user creation, system user reads and `PATCH /v2/admin/users/{id}/tenant`; none of the 11 snapshots specifies `GET/POST /v2/admin/tenants` or `PATCH/DELETE /v2/admin/tenants/{id}` | Publish the intended tenant administration contract, including list paging, identity/display/timezone fields, permissions, conflict versions and deletion constraints. Confirm whether those old routes exist or were retired. | The four unsupported legacy service methods and tenant CRUD form are removed. The Tenants tab states availability accurately. Supported user creation and migration accept an explicit existing tenant ID; the frontend never selects an inferred first tenant. |
| B15 | Tenant Admin visual Listening/Reading question authoring | `CreateListeningSectionRequest.kind` and `CreatePassageQuestionRequest.kind` are unrestricted strings; `payload` and `CreatePassageRequest.paragraphs` reference generic `JsonNode` | Supply supported question kinds, per-kind payload and paragraph schemas, examples and validation limits. Saved-section editing remains separately requested in `frontend-backend-notes-2026-09-02.md`. | Existing template/version/media/publish and Writing forms work. Listening/Reading retain their existing structured metadata plus advanced JSON content entry; this is **not** accepted as a finished customer-facing visual question editor. No guessed payload format is shipped. |
| B16 | Parent student names and consistent paginated reads | `ParentStudentLinkResponse` includes `studentUserId` but only the **parent's** name fields. The linked-student success schema declares an array; deployed compatibility reads and existing fixtures also use a page envelope. Academic reads remain generic. | Add student display identity to the authorized link projection; document array/page compatibility, page-size limits, totals and all academic payload schemas. Confirm whether report and learning collections paginate. | The selector uses real student IDs rather than mislabelling a parent's name as the student. It follows every returned link page, supports legacy arrays and rejects IDs outside returned links. Report pages, nested data and independent learning errors remain usable. |

## What is already available and must not be requested again

- Course discussion reads/writes and attachment operations exist. B03 asks for typed reads and reactions, not for creation of the existing endpoints.
- Student progress exists. B01 is a distinction between assignment completion and lesson playback progress.
- Personal events already support CRUD. The frontend now supplies the required UTC query window and current edit version.
- Parent links exist for Advisor, Counsellor and Tenant Admin. B05 concerns a **parent-specific Advisor messaging directory/thread**, not parent association.
- Published course reports, attendance histories, student study-plan task mutations, Instructor grading, Tenant governance, and Parent views already have separate frontend entry points. Keep those role boundaries.

## Acceptance evidence needed from backend owners

For each supported flow, provide a redacted request/response fixture or a designated Dev account within its intended role, and identify the checked-in contract revision it implements. In particular, a typed generic-read replacement should include a populated response, empty response, error response, pagination boundary and cross-student/tenant denial. Until such evidence exists, frontend fixture success is not an assertion of authenticated Dev success.
