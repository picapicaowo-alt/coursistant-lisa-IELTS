# 全站可用性复查：前端失败项修复

日期：2026-09-06（America/Los_Angeles）。范围：独立 IELTS 前端，Tokyo Production。

## 修复结论

上轮额外回归的 11 项失败均已在前端修复，并在本次发布候选与原工作区分别复跑通过。这是 11 个失败测试实例，归属于作业和模考的多个共同根因，不是 11 项后端故障。

| 原失败实例 | 修复后的行为 |
| --- | --- |
| 5 项作业提交组件回归 | 同一文件集合重试复用幂等键，换文件生成新键；上传及暂存回读完成前禁止提交；提交中禁止修改文件；暂存读取失败时禁止使用旧列表，并提供只读重试；提交已接受后只重读状态，不再次提交。 |
| 3 项英文／简体／繁体作业浏览器回归 | 首次上传完成后读取真实暂存列表，能够提交；提交后读取失败可以安全重试并刷新，恢复提示适配 390px。 |
| Reading 图题身份 | 使用 API 返回的 passage seq=3、question sortOrder=7，而非数组位置；对应图片与答题框能正常呈现。 |
| Writing 活跃答案保留 | 超过缓存期限后网络重连／焦点事件不再重载活跃答题会话，未提交文本保留。此项不承诺浏览器整页刷新后的草稿恢复。 |
| 模考部分媒体失败 | 等所有媒体读取成功、路由未取消后才分配 Blob URL；部分失败不留下孤立 URL，重试恢复题目。 |

同时修正本轮教师浏览器审查发现的四处前端问题：新增作业按钮的错误文案、日视图的错误标签、调课状态三语缺项，以及教师课务页通往学生专用成绩页的入口。教师继续使用每份作业的批改入口，未改变权限守卫。

所有新反馈使用共享 i18next 的英文、简体中文和繁体中文资源。未更改接口、环境变量、依赖锁文件、考试内容或后端实现。

## 验证与版本

候选已整合 main `4c774a624ebc5fb4387979f99adf230009768a00`，使用隔离工作区执行锁文件安装和检查。

- 作业组件 6/6 通过（包括原先通过的 1 项），新增浏览器 6/6 通过。
- lint、应用／Production／i18n 工具类型检查、语义翻译键、活跃 UI 翻译检查、6 项 i18n 工具测试、Production 构建通过。
- 单测 178 个文件、889 项全部通过。
- 完整 Chromium 回归 439 项全部通过，0 失败、0 跳过、0 重试，5.1 分钟（2 workers）。
- 原工作区单独重新构建并通过相同 6 项组件与 6 项浏览器用例，以及 lint 和 Production 类型检查；原工作区的其他未提交代码不作为发布源。

原工作区验证初次启动时依赖目录为空，四条命令在测试启动前报工具不存在；按原锁文件重新安装后重跑通过。此环境准备错误没有计为功能失败。候选构建保留既有大 chunk 提示。

机器可读结果和最终日志位于 `evidence/site-availability-remediation-20260906/`。上轮失败原始记录保留在原工作区 `output/site-availability-20260906/`；本轮完整原始输出在 `output/site-availability-prod-20260906/`。

## 真实验收与后端交接边界

本轮发布前恢复了 Production Instructor 的真实会话，教学首页和已有课程读取得到真实内容。上述作业上传／提交及模考试卷失败场景使用本地 fixture；尚未取得 Student 可写 QA 资源，不能宣称真实学生上传、评分和跨角色持久化已全部通过。

没有发现符合“实质影响用户体验、正确调用且有本轮真实响应证据”的新增后端问题，因此本次不生成后端故障单，也不将模拟 404／503 或未验收项目交给后端。此前候选的泛化后端核验文档已改为 [前端内部真实验收清单](client-functional-live-acceptance-checklist-2026-09-05.md)。

发布验收单独记录正式合并 SHA、不可变快照和线上哈希、CloudFront 状态及真实教师读操作／刷新／退出，不能用本地测试替代真实角色验收。
