/** Route/state ids stay stable; presentation follows the shared locale resources. */
export const MY_OPERATIONS_SECTION_KEYS = {
  overview: 'advising:studentPlan.overview',
  teaching: 'operations:teacher.tabs.teaching',
  availability: 'operations:teacher.tabs.availability',
  calendar: 'common:sidebar.calendar',
} as const;
