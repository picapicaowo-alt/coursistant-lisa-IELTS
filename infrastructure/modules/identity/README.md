# Identity module

Creates the backend-developers IAM group and a least-privilege policy for infrastructure inspection, application-bucket operations, secret metadata inspection, and Session Manager access to tagged pilot instances. It creates no IAM users, passwords, or access keys; users are added through the team's identity lifecycle process.
